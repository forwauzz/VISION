# Vision — copy to .env and fill values. Do not commit .env.
# VITE_API_BASE_URL=
# Optional: Phase 6 transcription proxy (server). Run: npm run server
# DEEPGRAM_API_KEY=your_deepgram_key
# TRANSCRIPTION_PORT=3009
# Optional: Batch Vision at end of session (frame descriptions). POST /api/vision/describe-frames
# OPENAI_API_KEY=your_openai_key
